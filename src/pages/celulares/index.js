import { cleanup } from "@testing-library/react";
import { useParams } from "react-router";
import { Link } from "react-router-dom";

export function Celulares() {
  
}

export default Celulares;
/*
    <p>{getCategoria(categoryId).image}</p>
*/